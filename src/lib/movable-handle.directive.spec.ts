import { MovableHandleDirective } from './movable-handle.directive';

describe('MovableHandleDirective', () => {
  it('should create an instance', () => {
    // const directive = new MovableHandleDirective();
    // expect(directive).toBeTruthy();
  });
});
